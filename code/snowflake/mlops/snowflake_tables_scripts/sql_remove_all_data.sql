-- Remove all data from the ACTOR table
DELETE FROM HACK_DATA.PUBLIC.ACTOR;

-- Remove all data from the REPO table
DELETE FROM HACK_DATA.PUBLIC.REPO;

-- Remove all data from the ORG table
DELETE FROM HACK_DATA.PUBLIC.ORG;

-- Remove all data from the GLOBAL_EVENTS table
DELETE FROM HACK_DATA.PUBLIC.GLOBAL_EVENTS;

-- Remove all data from the GITHUB_EVENTS_WIDE table
DELETE FROM HACK_DATA.PUBLIC.GITHUB_EVENTS_WIDE;